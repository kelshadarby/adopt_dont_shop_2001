<h1>All Pets<h1>

<% @pets.each do |pet| %>
  <img src="<%= pet.image %>" alt="Pictured: <%= pet.name %>">
  <h2><%= pet.name %></h2>
  <p>Approximate Age: <%= pet.approximate_age %></p>
  <p>Sex: <%= pet.sex %></p>
  <p>Currently at: <%= pet.shelter.name %></p>
<% end %>
